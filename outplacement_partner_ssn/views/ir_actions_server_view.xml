<?xml version="1.0"?>
<odoo>
    <record id="load_ais-f_data" model="ir.actions.server">
        <field name="name">Load social security numbers</field>
        <field name="model_id" ref="base.model_ir_actions_server"/>
        <field name="state">code</field>
        <field name="code">
partners = env['res.partner'].search([('company_registry', '!=' False)])

for partner in partners:
    partner.write({'social_sec_nr': partner.company_registry})
        </field>
    </record>
</odoo>