<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Extra groups needs to assign for normal user to enable hides menu of accounting dafa read user -->
    <record id="group_disable_dafa_hide" model="res.groups">
        <field name="name">Disable Dafa hide</field>
    </record>


    <record id="sale_order_line_see_all" model="ir.rule">
        <field name="name">All Orders Lines</field>
        <field ref="model_sale_order_line" name="model_id"/>
        <field name="domain_force">[(1,'=',1)]</field>
        <field name="groups" eval="[(4, ref('sales_team.group_sale_salesman_all_leads')),
        (4, ref('base_user_groups_dafa.group_dafa_admin_accounting_read'))]"/>
    </record>

</odoo>
